import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1
  },
  headerText: {
    fontSize: 20,
    textAlign: 'center',
    marginTop: 20,
  },
  flashcard: {
    height: 40,
    margin: 12,
    borderWidth: 1,
  }
})

export {styles}
